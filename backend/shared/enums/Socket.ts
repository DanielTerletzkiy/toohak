export enum SocketPath {
  Lobby = 'lobby',
  User = 'user',
  Answer = 'answer',
}

export enum SocketAction {
  Update = 'update',
  Create = 'create',
  Remove = 'remove',
  Join = 'join',
  Leave = 'leave',
  Vote = 'vote',
}
